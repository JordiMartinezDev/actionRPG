class MainChar {
  constructor(level, hp, attack, isGodModeEnabled) {
    this.level = level;
    this.hp = hp;
    this.attack = attack;
    this.isGodModeEnabled = isGodModeEnabled;
  }
  baseAttack() {}
  blink(distance) {}
}
